using System;

public class Transport
{
    public static void Main()
    {
        int peaple = int.Parse(Console.ReadLine());
        int capacity = 24;

        double courses = (double)peaple / capacity;
        Console.WriteLine("{0}",Math.Ceiling(courses));
    }
}
