using System;
using System.Linq;

namespace ReverseString
{
    public class ReverseString
    {
        public static void Main()
        {
            var test = Console.ReadLine();
            test.ToList();
            var reversedTest = test.Reverse();

            Console.WriteLine(string.Join("", reversedTest));
        }
    }
}
