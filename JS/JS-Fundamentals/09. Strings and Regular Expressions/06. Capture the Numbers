function solve(array) {

    let text = [];
    for (let i = 0; i < array.length; i++) {
        text += array[i];
    }

    let regex = /\d+/g;
    let matchs = text.match(regex);

    console.log(matchs.join(' '));
}
