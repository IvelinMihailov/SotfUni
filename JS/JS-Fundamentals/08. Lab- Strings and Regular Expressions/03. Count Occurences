function solve(word,text) {

    let count = 0;
    let index = text.indexOf(word);

    while (index >= 0){
        index = text.indexOf(word,index+1);
        count++;
    }

    console.log(count);
}
