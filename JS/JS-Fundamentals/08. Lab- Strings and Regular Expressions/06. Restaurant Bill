function solve(arr) {

    let purchases = arr.filter((e,i) => i % 2 == 0);
    let sumPrice = arr.filter((e,i) => i % 2 == 1).map(Number).reduce((a,b) => a+b);

    console.log(`You purchased ${purchases.join(', ')} for a total sum of ${sumPrice}`);
    
}
