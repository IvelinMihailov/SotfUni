function solve(w, h, W, H) {

    let figureOne = w * h;
    let figureTwo = W * H;

    let diffrence = Math.min(w,W) * Math.min(h,H);
    let result = (figureOne + figureTwo) - diffrence;
    debugger;
    console.log(result);
}
